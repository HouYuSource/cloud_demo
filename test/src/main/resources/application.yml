server:
  port: 8081
spring:
  application:
    name: test

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6868/eureka/
  instance:
    prefer-ip-address: true

feign:
  hystrix:
    enabled: true